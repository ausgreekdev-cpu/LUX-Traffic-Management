BFC Shine — drop licensed files here:
- BFCShine.woff2 (web, preferred)
- BFCShine.woff  (fallback)
- BFCShine.ttf  (PDF embedding, also triggers @font-face)
- BFCShine.otf  (alternative)

The app self-hosts via @font-face in src/index.css and via Branding API slot font_display.
Upload via Branding > Typography & Assets > Feature font — BFC Shine for per-domain white-label.
Until files land, --font-display falls back to cursive/serif.
